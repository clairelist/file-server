onSubmit(arg)=>{
	axios.post('fakeapi.com/post', arg)
	.then(res=>{
	setFormResponseState(res.data);
	}).catch(err=>{
	console.error(err);
	})
}


<App>
<Calendar ... />
<Form submit={onSubmit(date)} formResponse={formResponseState} />
</App>

...>>>

const initialFormConfirms = {
	headerConfirm: 'congratulations, name!',
	bodyConfirm: 'your date is confirmed!'
};

const [formResponseState, setFormResponseState] = useState(initialFormConfirms);

Form

	<div classname='form-actual'>
	<lable, form, etc, for... onSubmit={onSubmit}
	value={value}
	placeholder='placeholder'
	...
	/>
	<button onClick={onSubmit(form.value)} />

	<div classname='response'>
	<h1>formResponse.headerConfirm !</h1>
	<p>formResponse.bodyConfirm !</p>
	</div>