{photos.map(photo => (
            <div className='photos-wrapper' key={photo.date}>
               <img src ={photo.url.medium} alt={`Nasa APOD for ${photo.date}`}/>
            </div>
            ) 
            )}


//100 days ago goodness
let today= new Date();
today.setDate(today.getDate());

let one_hundo = new Date();
one_hundo.setDate(one_hundo.getDate() - 100 );

//NEXT, pull out year, month, and day from one_hundo, and format using
//string concatenation (add strings togethers) with a - in between.
//take care that each much start with 01 not 1 ! FUN :-(